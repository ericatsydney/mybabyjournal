machine:
  pre:
    - sudo apt-get update; USE_PRECOMPILE=true sudo -E circleci-install php 7.1.0
  php:
    version: 7.1.0

test:
  override:
    - php --version
    - ./vendor/bin/phpunit tests/ci

deployment:
  production:
    branch: master
    commands:
      - ssh root@139.59.113.228 /root/scripts/pull-master.sh
